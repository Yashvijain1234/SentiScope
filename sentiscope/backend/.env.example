# Port the Node/Express API listens on
# (5000 is avoided because macOS AirPlay Receiver uses it by default)
PORT=5050

# Base URL of the Python ML microservice
ML_SERVICE_URL=http://localhost:8000

# Comma-separated list of allowed CORS origins (the React dev server)
CORS_ORIGIN=http://localhost:5173
